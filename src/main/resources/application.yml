spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/beginner?serverTimezone=Asia/Seoul
    username: root
    password: chankyo@6050

  jpa:
    properties:
      hibernate:
        format_sql: true
    show-sql: true
    hibernate:
      ddl-auto: update


logging:
  level:
    org.hibernate.SQL: debug